<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Homework App" %></title>

    <%= stylesheet_link_tag    "application" %>
    <link rel="stylesheet" href="//fonts.googleapis.com/css?family=Open+Sans:400,300,600,700,800" />
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <!-- Title Area -->
        <li class="name">
          <h1><a href="/">Homework Tracker</a></h1>
        </li>
        <li><div class="toggle-topbar menu-icon"><span>Menu</span></div></li>
      </ul>

      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
            <% if user_signed_in? %>
                <li class="divider hide-for-small"></li>
                <li><%= link_to 'Sign out', destroy_user_session_path, method: :delete %></li>
            <% else %>
                <li><%= link_to "Sign in", new_user_session_path %></li>
                <li><%= link_to "Sign up", new_user_registration_path, {class: 'signup-nav'} %></li>
            <% end %>
        </ul>
      </section>
    </nav>

  <body>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>